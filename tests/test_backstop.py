"""STA+24h backstop forces a terminal close and deregistration when no 'landed' is received."""
from __future__ import annotations

import pytest

# Milestone 7 (logic can be unit-tested earlier if extracted as a pure helper).
